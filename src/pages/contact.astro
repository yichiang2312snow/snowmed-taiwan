---
import BaseLayout from '../layouts/BaseLayout.astro';
import { SITE, SITE_A } from '../consts';

const channels = [
  { label: '電子郵件', value: SITE.email, href: SITE.email ? `mailto:${SITE.email}` : null },
  { label: 'LINE 官方帳號', value: SITE.lineId, href: null },
  { label: 'Instagram', value: SITE.instagram, href: SITE.instagram },
  { label: 'Facebook', value: SITE.facebook, href: SITE.facebook },
].filter((c) => c.value);
---

<BaseLayout
  title="聯絡與預約"
  eyebrow="Contact"
  description="與楊怡強醫師聯絡，預約滑雪行前評估、運動傷害門診或回歸雪場諮詢。"
>
  <section class="bg-panel">
    <div class="mx-auto max-w-3xl px-5 py-16 sm:py-20">
      <p class="text-lg leading-relaxed font-medium text-head">
        歡迎來信討論行前準備、傷後復原，或任何和雪地運動有關的醫療問題。
      </p>

      {
        channels.length > 0 ? (
          <dl class="mt-10 divide-y divide-line overflow-hidden rounded-2xl border border-line">
            {channels.map((c) => (
              <div class="flex flex-wrap items-center justify-between gap-3 bg-surface px-6 py-5">
                <dt class="text-sm font-bold tracking-widest text-accent uppercase">{c.label}</dt>
                <dd class="text-sm text-head">
                  {c.href ? (
                    <a href={c.href} class="font-bold hover:underline">
                      {c.value}
                    </a>
                  ) : (
                    c.value
                  )}
                </dd>
              </div>
            ))}
          </dl>
        ) : (
          /* 還沒公開 email 或社群帳號時，給讀者一條真的走得通的路，
             而不是一個「尚未設定」的空框。等 consts.ts 填上就會自動換成上面那張表。 */
          <div class="mt-10 space-y-4">
            <div class="min-w-0 rounded-2xl border border-line bg-surface p-6">
              <p class="font-black text-head">想問工具或網站的事</p>
              <p class="mt-2 text-sm leading-relaxed break-words text-muted">
                最快的方式是到
                <a href="/wishlist" class="font-bold text-accent hover:underline">許願留言區</a>
                留言 —— 免註冊、可匿名，我會定期看。想要什麼新工具、哪裡用起來卡卡的、資料有誤要更正，都寫在那裡。
              </p>
            </div>

            <div class="min-w-0 rounded-2xl border border-line bg-surface p-6">
              <p class="font-black text-head">想問門診、個別化的醫療問題</p>
              <p class="mt-2 text-sm leading-relaxed break-words text-muted">
                個別化的醫療建議沒辦法在網路上給。門診資訊與聯絡方式請見
                <a
                  href={SITE_A.aboutUrl}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="font-bold text-accent hover:underline"
                >
                  {SITE.doctor}的個人網站
                </a>
                ，上面也有更深入的衛教專文。
              </p>
            </div>

            <div class="min-w-0 rounded-2xl border border-line bg-surface p-6">
              <p class="font-black text-head">人在國外雪場、現在就受傷了</p>
              <p class="mt-2 text-sm leading-relaxed break-words text-muted">
                不要等回信。先看
                <a href="/emergency" class="font-bold text-accent hover:underline">海外受傷緊急處置</a>
                ：急救電話、雪場巡邏隊、附近的醫院與理賠要準備的文件都在那一頁。
              </p>
            </div>
          </div>
        )
      }

      <div class="mt-12 rounded-2xl border-l-4 border-accent bg-tint1 p-6">
        <h2 class="font-black text-head">來信前，這些資訊會很有幫助</h2>
        <ul class="mt-3 space-y-2 text-sm leading-relaxed text-muted">
          <li>· 你玩的是單板還是雙板、滑齡多久、大概什麼程度</li>
          <li>· 如果是受傷：怎麼摔的、哪一天、當下有沒有聽到聲音或腫起來</li>
          <li>· 在國外有沒有就醫？有沒有診斷書或影像光碟？</li>
          <li>· 你的目標：下個雪季想滑什麼難度、什麼時候出發</li>
        </ul>
      </div>

      <p class="mt-10 text-xs leading-relaxed text-muted">
        提醒：網路訊息無法取代正式的醫療診察。若有急性外傷、劇烈疼痛、肢體無力或麻木等狀況，請直接就醫或撥打 119。
      </p>
    </div>
  </section>
</BaseLayout>
