---
interface Props {
  tone?: 'snow' | 'white' | 'ice';
  class?: string;
}
const { tone = 'snow', class: cls = '' } = Astro.props;
const bg = { snow: 'bg-surface', white: 'bg-panel', ice: 'bg-white/8' }[tone];
---

<section class={`${bg} ${cls}`}>
  <div class="mx-auto max-w-5xl px-5 py-14 sm:py-20">
    <slot />
  </div>
</section>
